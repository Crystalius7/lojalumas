# prospect lists contain real emails — never publish them to the public repo
prospects-*.txt
*.csv
node_modules/

# sender credentials, send history and reply data — never commit
sender/.env
sender/sent-log.json
sender/.token.json
sender/replies-pending.json
sender/declined.json
sender/agent-log.md
sender/agent-last-run.log
sender/active-clients.json
